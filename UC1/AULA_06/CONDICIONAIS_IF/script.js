let valor = 100
let regra = 100

if (valor === regra) {
    console.log(`o valor ${valor} corresponde a ${regra}`)
} else {
    console.log(`o valor ${valor} não corresponde a  ${regra}`)
}

//Atividade (01)

let Fatu = 32520.96
let Meta = 45630.00

if (Fatu === Meta) {
    console.log(`Se o faturamento ${Fatu} atingiu a meta esperada de ${Meta}`)
} else {
    console.log(`o faturamento ${Fatu} não atingiu a meta esperada de ${Meta}`)
}