//números e operados
let v1 = 10
let v2 = 5

const totalsoma = v1 + v2
const totalsubtracao = v1 - v2
const totalmultiplicacao = v1 * v2
const totaldivisao = v1 / v2

let valorsoma = totalsoma.toLocaleString("pt-BR", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
});
let valorsoma1 = totalsoma.toLocaleString("pt-BR", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
});
let valorsoma2 = totalsoma.toLocaleString("pt-BR", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
});let valorsoma3 = totalsoma.toLocaleString("pt-BR", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
});

console.log("OPERAÇÕES")
let num = 0
num ++




console.log(`VALORES

    VALOR 1: ${v1} e VALOR 2: ${v2}
    RESULTADO SOMA: ${valorsoma}
    RESULTADO SUBTRAÇÂO: ${valorsoma1}
    RESULTADO MULTIPLICAÇÂO: ${valorsoma2}
    RESULTADO DA DIVISÂO: ${valorsoma3}

    `)

    //ATIVIDADE (teste1)
    //produto 

let produto = "computador"
let valor = 22566.00
let desconto = 750.00
const vf = valor - desconto

let valorsoma5 = vf.toLocaleString("pt-BR", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
});
let valorsoma6 = valor.toLocaleString("pt-BR", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
});
let valorsoma7 = valor.toLocaleString("pt-BR", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
});

console.log(`VALORES
    VALOR 1: ${valorsoma6}
    DESCONTO: ${valorsoma7}
    VALOR FINAL: ${valorsoma5}
     
    `)


